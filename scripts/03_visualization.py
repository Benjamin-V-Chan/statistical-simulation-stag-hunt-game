# Import necessary modules (pandas, matplotlib.pyplot, os).
# Define the path to the simulation CSV (outputs/simulation_results.csv).
# Read the CSV file into a DataFrame.
# Create a line plot for the evolution of "fraction_stag" over generations.
# Save the figure to outputs (e.g., outputs/fraction_stag_plot.png).
# Create a second line plot for "avg_payoff" over generations.
# Save the second figure to outputs (e.g., outputs/avg_payoff_plot.png).
# Optionally display the plots.